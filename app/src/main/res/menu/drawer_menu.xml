<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Main menu with submenu -->
    <item
        android:id="@+id/nav_db"
        android:icon="@drawable/ic_db"
        android:title="Directus settings"
        android:checkableBehavior="single">

        <!-- Submenu -->
        <menu>
            <item
                android:id="@+id/nav_db_option1"
                android:title="Option 1" />
            <item
                android:id="@+id/nav_db_option2"
                android:title="Option 2" />
        </menu>
    </item>

    <!-- Other main menus -->
    <item
        android:id="@+id/nav_printer"
        android:icon="@drawable/ic_printer"
        android:title="Printer settings" />
    <item
        android:id="@+id/nav_about"
        android:icon="@drawable/ic_info"
        android:title="About us" />
    <item
        android:id="@+id/nav_help"
        android:icon="@drawable/ic_help"
        android:title="Help" />
</menu>
